meta {
  name: Download Document (log access)
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/documents/{{documentId}}/download
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
