meta {
  name: List Projects
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/projects
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

params:query {
  clientId: {{clientId}}
}
