meta {
  name: Reset User Password
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/admin/users/{{userId}}/reset-password
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "newPassword": "Reset@12345"
  }
}
