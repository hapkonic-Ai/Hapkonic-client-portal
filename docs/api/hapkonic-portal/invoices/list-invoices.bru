meta {
  name: List Invoices
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/invoices
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

params:query {
  clientId: {{clientId}}
}
