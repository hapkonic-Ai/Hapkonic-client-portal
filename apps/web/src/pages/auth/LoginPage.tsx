export default function LoginPage() {
  return <div className="flex h-screen items-center justify-center" style={{ background: 'var(--bg-base)' }}><p style={{ color: 'var(--text-primary)' }}>Login Page — Phase 4</p></div>
}
